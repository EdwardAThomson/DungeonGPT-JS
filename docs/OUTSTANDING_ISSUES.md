# Outstanding Issues

Triage doc consolidating every known open item across the `docs/` folder and session discussions. Items removed when done. Decisions column (`Soon / Defer / Skip`) is blank — to be filled in during prioritization.

Source audits: roadmap survey (2026-04-19), auth verification (2026-04-19), docs consolidation (2026-04-17 through 2026-04-19). Auth system and RAG system were surveyed and confirmed shipped — removed from this list.

---

## Priority queue (working order, 2026-07-16)

The ordered to-do list over the open rows below. Re-sort as decisions land and strike ranks as they complete; this section supersedes the 2026-07-03 parallelization map for *ordering* (that section remains the cluster/file-ownership record). Sizing and full context live in each numbered row.

**P0 — clear the decks (small, do first):**
1. **#1 magic-link OTP template fix** (XS): live users are actively getting stuck; a 5-minute dashboard edit.
2. **Deploy-state close-out audit** (S): the doc carries many "BUILT (pending review + manual migration)" rows from early July, and the git log shows most have since merged (PRs through #135, including hub payments Phase 1). Confirm migrations `002`-`006` are applied to the data Postgres, confirm #39 / #40 / #54 P3 / #62 / #72 / #73 P1 / #6-codes are live, and mark those rows Done here. The entire premium stack is built; unrealized value until this is verified.

**P1 — strategic, maintainer-flagged:**
3. **#82 Track A: logged-out landing/marketing page** (M): flagged "arguably the most important"; the determinism-credibility pitch is undercut by a weak site, and it is self-contained. Tracks B/C follow later, not now.
4. **#30 (admin): read-only support dashboard** (M-L): flagged HIGH PRIORITY since 2026-06-28; the next lost-hero support incident is currently unanswerable.

**P2 — the engine-referee program (one coherent arc, in order):**
5. **#76 narration contract implementation** (M): kills the marker-misfire class, prerequisite for the two rows below; the renderer-mode model eval (and the premium default-model decision) rides it.
6. **#83 skill checks, Phases 1-2** (M of the M-L): HIGH PRIORITY; Phase 1 explicitly rides #76's DM_PROTOCOL rewrite, so schedule them together (same prompt surgery, one few-shot exemplar bank).
7. **#77 AI quest generation** (L): rests on the #76 contract; design done, Phase 0 gate already shipped.

**P3 — revenue completion:**
8. **#6 billing: continue hub payments** (L): Phase 1 (worker reads the Octonion hub billing tier) merged 2026-07-15; remaining is checkout/subscription rails + usage accounting. Everything else in the money stack (#39, #40, #7, #12, redemption codes) is already built, so this is the last gate to charging.
9. **#65 Phase 5: premium packaging** (S-M): load the flagship canal template into `premium_templates` + the locked NewGame card. First real premium SKU on the shelf; do immediately after (or alongside) payments.

**P4 — product feel (parallel-safe with P2/P3, schedule by taste):**
10. **#79 keystone refactor** (extract `useEncounterFight`) then Thread B modal refine.
11. **#78 Phases 1-2** (button glow/danger states, then SFX manager + core sounds).
12. **#80 mood theming, lever A** (per-tile mood dressing for grimdark/eldritch).
13. **#81 TTS Phase 1** (Workers AI Aura + R2 hash cache).
14. **#73 arc cards Phases 2-4** (taglines/art, meta-narrative copy, capstone).

**P5 — backburner / deliberate re-triage:**
- #9 AI map tiles, #16 structured logging: fine anytime, not urgent.
- #5 layered terrain and #60 remaining steps: fold into the #66 realistic-worlds direction rather than doing piecemeal; #60's entitlement gate also wants P0's deploy confirmation plus demand evidence.
- Combat-depth table #27-#33 remainder: re-triage deliberately post-#43 as the doc already says; several are likely "decide NOT to build" (they add 5e fidelity we deliberately don't chase).
- #31 battlemaps, #32 multiplayer, #66 realistic worlds: big bets, own tracks, no dates.

---

## Critical — data loss or deployment blockers

| # | Issue | Source | Size | Decision |
|---|---|---|---|---|
| 1 | **Supabase magic-link email includes an OTP code but UI has no input for it.** Users who try to enter the code are stuck. Fix: edit Supabase email template to remove `{{ .Token }}` (5 min dashboard change) — don't build OTP UI, since auth is centralized at Octonion. | Session 2026-04-19 | XS (dashboard) | |

> ~~Previously listed: Supabase conversations schema gaps~~ — **verified 2026-04-19 as RESOLVED.** All 6 fields (`game_settings`, `selected_heroes`, `summary`, `world_map`, `player_position`, `sub_maps`) are in the schema, saved, and hydrated. AI_PROMPT_ANALYSIS.md §5 is stale.

---

## Large initiatives — multi-day / multi-week features

| # | Issue | Source | Size | Decision |
|---|---|---|---|---|
| 3 | ~~**Team encounter system (Lead + Support).** Full party combat for multi-hero parties. Design complete, zero code. Now the core of the combat-depth program (#43, [T3_CAMPAIGNS_PLAN.md](T3_CAMPAIGNS_PLAN.md) §7).~~ **SHIPPED** — Lead + Support went live in the #43 combat-depth rework (2026-07-03): formation phase, support bonuses in the roll breakdown, KO/auto-swap. | [ENCOUNTER_SYSTEM.md](ENCOUNTER_SYSTEM.md) Phase 5 | L | |
| 5 | **Layered terrain generation.** Noise-based heightmaps with biome quantiser, rivers, erosion. Prototype at `src/experimental/mapGen/layeredGenerator.js` exists but not wired to production. | [TERRAIN_ROADMAP.md](TERRAIN_ROADMAP.md) | L | |
| 6 | **Billing + usage accounting.** No credit ledger, no `ai_usage_events` table, no Lemon Squeezy (or alternative) integration. **FIRST SLICE (redemption codes) BUILT 2026-07-07 (pending review + manual migration `006_redemption_codes.sql`): the billing MVP shipping before payment rails (maintainer 2026-07-06/07).** Codes grant time-boxed membership (`grants_tier` member/premium/elite × `grant_days`), carry mandatory expiry + `max_uses` + a `disabled` kill switch (no immortal codes; minted in the private admin panel in small noted batches). Worker: `POST /api/db/redeem-code` (atomic claim in one transaction, generic `code_invalid` for every dead-code flavour, distinct 409 `already_redeemed`, per-user 10/day attempt limit that fails CLOSED); `getAccountTier` became effective-tier (base `account_tiers` MAX active `tier_grants`, passive expiry, never lowers the base) so entitlements/premium-templates/premium-AI all honour grants automatically; `GET /entitlements` gained additive `expiresAt`. Client: Redeem-a-code field on the Profile page, post-redeem tier refresh without re-login, grant end date on the Membership row. See [REDEMPTION_CODES.md](REDEMPTION_CODES.md). Remaining for #6 proper: payment rails (top-ups/subscriptions), usage accounting/credit ledger. | [DEPLOYMENT_ARCHITECTURE.md](DEPLOYMENT_ARCHITECTURE.md) §5, [REDEMPTION_CODES.md](REDEMPTION_CODES.md) | L | First slice built (pending review + migration 006) |
| 7 | **OpenRouter premium tier.** **BUILT 2026-07-06 (pending review + manual migration; tier gating rides #39 entitlements, not #6 billing).** `POST /api/ai/generate` accepts `pool: 'free' \| 'premium'` (unknown = free); premium requires member+ (`account_tiers`, 403 `premium_required` otherwise) and passes a DAILY allowance (bucket `ai-premium-daily` in the #12 counter table: 100/day member, 200/day premium, 300/day elite, inside monthly allowances of 800/2000/4000; over = 429 `premium_cap`). Curated pool in `cf-worker/src/services/openrouter.ts` (`openai/gpt-5-mini` default, `anthropic/claude-haiku-4.5`, `google/gemini-3.5-flash`; same fallback + SAME `sanitizeResponse` as the free pool). OpenRouter failure/missing key falls back to the free pool automatically, marked `fallbackFrom: 'premium'` — never a dead generation. Client: Premium AI chip goes live for member+ (`src/components/Modals.js` + `src/services/aiPool.js`, preference persisted, guests stay locked), `llmService` sends the gated pool, retries once on cap/refusal, and the AI tab quietly surfaces pool-used / cap-fallback. Harness: `scripts/test-cf-premium-ai.mjs` (manual). **Deploy runbook: apply `cf-worker/migrations/005_request_counters.sql` via psql FIRST, then `npx wrangler secret put OPENROUTER_API_KEY`, then deploy the worker.** | [CF_WORKER_GUIDE.md](CF_WORKER_GUIDE.md) AI pools section | M-L | Done (merged 2026-07-06, PR #52; deploy prereqs: migration 005 + `OPENROUTER_API_KEY` secret) |
| 30 | **Admin dashboard — read/inspect the data Postgres (self-hosted, formerly Supabase).** *(Flagged HIGH PRIORITY.)* No way today to view player saves / heroes / conversations for support or debugging. Triggered by a player who lost a hero to the duplicate-hero bug and asked whether their character still exists in the DB — which we cannot currently confirm. Needs a read view over conversations/saves (auth-gated to an admin), at minimum read-only. | Session 2026-06-28 | M-L | Soon | 
| 43 | ~~**Combat-depth program: bosses hit back + party boss fights.**~~ **SHIPPED 2026-07-03.** Lead + Support (Phase 5) is live for multi-round fights (support bonus in the roll, lead KO auto-swap, crit-fail splash, party XP split); enemy damage is FLAT per outcome with HP-scaled `maxRounds` (enemy HP is a real knob); all 10 template bosses carry explicit `dealsDamage`/`damage` profiles (keyword matching deprecated, kept as old-data fallback); the three deadly t2 bosses retuned to sim-validated `dc` 19-20. All boss pins in `progressionLint.test.js` healed (bands: t1 solo / t2+ 3-hero mid-gear). Shipped note in [ENCOUNTER_SYSTEM.md](ENCOUNTER_SYSTEM.md) Phase 5; #3 shipped with it. | Design session 2026-07-03 | L | Done |

> ~~#4: Narrative milestones — conversation-gated quest outcomes not built.~~ — **SHIPPED (verified 2026-07-03 audit).** Both halves now exist end-to-end: AI-judged `type: 'narrative'` milestones complete via the `[COMPLETE_MILESTONE]` marker in `useGameInteraction.js` (guarded so a stray marker can never complete a mechanical milestone; 5 narrative milestones live in `storyTemplates.js`), and the 2026-07-02 milestone NPC-grounding work added a deterministic **`talk` mechanical type** (`npc_talked` events, Option C) with authored, placed NPCs (Captain Marta / Ulric) — the conversation-gated outcome the row asked for. Design record: [MILESTONE_NPC_GROUNDING_PLAN.md](MILESTONE_NPC_GROUNDING_PLAN.md).

---

## Medium — scoped features, asset generation, integrations

| # | Issue | Source | Size | Decision |
|---|---|---|---|---|
| 8 | ~~**Richer loot line (de-scoped 2026-07-04: NO AI narration — not worthwhile).**~~ **DONE 2026-07-06.** The flat "+50 XP, +12 gold" chat lines (milestone reward summary, side-quest/turn-in payouts, site loot) are now seeded templated sentences: `src/game/rewardNarrator.js`, localNarrator-style content-seeded pick from 4 shapes, e.g. "Victory yields 200 XP for each party member, 100 gold and the Ancient Amulet." Data identical; the machine-formatted rewardMessages arrays and the 💰/🎉 markers are untouched (call sites narrate a display copy only). Rarity color skipped: these are plain system lines. | ENCOUNTER_SYSTEM Phase 4 → maintainer de-scope | XS | Done |
| 9 | **AI image tiles for world map.** Phase 1: 7 easy tiles (features + towns, no edge-matching). Prompts written in [IMAGE_GENERATION_PROMPTS.md](IMAGE_GENERATION_PROMPTS.md); generation not run. | [TERRAIN_ROADMAP.md](TERRAIN_ROADMAP.md) | M (generation + wiring) | |
| 11 | **Automated CF Worker deploy.** Frontend auto-deploys via Pages; Worker deploy is manual (`wrangler deploy`). Needs `.github/workflows/deploy.yml` + post-deploy smoke test. | [DEPLOYMENT_ARCHITECTURE.md](DEPLOYMENT_ARCHITECTURE.md) §5 | S-M | Done (merged 2026-07-06): deploy-worker.yml with migration guard + smoke step |
| 12 | **Rate limiting on Worker.** **BUILT 2026-07-06 (pending review + manual migration).** Fixed-window per-user counters in Postgres (`request_counters`, migration `cf-worker/migrations/005_request_counters.sql` — the maintainer applies it via psql BEFORE deploying the worker; limiter fails OPEN so a missed migration degrades to no-throttling, never an outage). One atomic upsert-increment per counted request (`middleware/rateLimit.ts`). Buckets: `ai-generate` 30/5min free, 60/5min member+ (lazy tier lookup only past the free limit); `embed` 60/5min; `db-write` 120/5min on mutating `/api/db/*`; GET db reads deliberately unthrottled. 429s carry `{ code: 'rate_limited', bucket, retryAfterSeconds }` + `Retry-After`. Shares the counter table with #7's daily premium allowance. Limits are code constants — tuning needs no migration. | [CF_WORKER_GUIDE.md](CF_WORKER_GUIDE.md) rate-limiting section | S-M | Done (merged 2026-07-06, PR #52; deploy prereq: migration 005) |
| 44 | ~~**Gear-ladder expansion.** Fill the empty very_rare weapon rung and thin armor ladder (no legendary armor), add the t3 legendary shelf, shift the top end from quest artifacts to findable loot; bonuses sim-tuned, depends on #43 making defense matter. [T3_CAMPAIGNS_PLAN.md](T3_CAMPAIGNS_PLAN.md) §5.~~ **SHIPPED** — wave 3 (2026-07-03): runic_greatsword +2 rung, findable top-end, t3 legendary shelf authored+pinned, full icon art landed. | Design session 2026-07-03 | S-M + icon art | |
| 45 | ~~XP economy — side-quest backfill into in-progress saves.~~ **DONE 2026-07-04** — saves top up to cap from the grown pool on load (pool-size guard, deterministic, one subtle rumour line); pool expansion shipped wave 3 | Design session 2026-07-03 | M | |
| 50 | ~~Mid/top band — desert-expedition + frozen-frontier t2 sequels.~~ **DONE 2026-07-04** — "The Waking Sands" + "The Hungering Thaw" authored same-world on t1 geography, sim-validated (~48% mid / ~1% TPK), premium-gated; every genre now chains t1→t2 in-save | Design session 2026-07-03 | M | |
| 51 | ~~**In-game codex: item compendium + bestiary (discovered-only).**~~ **SHIPPED 2026-07-03.** Codex tab in the Adventure Book (#52): Bestiary (storyTemplates bosses + QUEST_ENEMIES + encounter-table hostiles) and Items (ITEM_CATALOG), all auto-generated (`src/game/codexEngine.js`), spoiler-light (name/art/flavor/rarity-tier only — no HP/DCs/drops). Discovered-only: "???" silhouettes until met; discovery recorded additively in `settings.codex` at acquisition/encounter-resolution chokepoints, with a throttled "📚 New codex entry" system line; old saves seed from current inventory/equipment on load; guests fully supported. | Design chat 2026-07-03 | M | Done |
| 52 | ~~**Unified tabbed modal hub ("Adventure Book").**~~ **SHIPPED 2026-07-03.** `AdventureBook.js` hub (modal id `adventureBook`, navigation group — encounters still auto-close it): 📜 Campaign / 🗺️ Side Quests / 📚 Codex (#51) / 🎒 Party / ⚙️ AI in the Journal's fixed min(82vh, 860px) frame, remembered last-tab per session. Absorbed the boolean-state Journal (`StorySettingsModalContent`, removed) and `PartyInventoryModal` (now the Party tab content, Items/Loadout intact). Transactional modals stay separate. **Map stays standalone**: click-to-move-while-open (#25) depends on Game.js's close/reopen-around-encounters flow, which a hub tab would degrade (decision recorded in AdventureBook.js). Map/Building remain the last boolean-state modals. | Design chat 2026-07-03 | M-L | Done |
| 53 | ~~Re-sim the t3 QUEST_ENEMIES band.~~ **DONE 2026-07-04** — all five tuned into the 40-60% band (elder_wyrm aligned to the flagship's validated profile); three t2 finale-mirrors deliberately left matching their inline blocks with a minLevel-bump recommendation instead | t3 authoring 2026-07-03 | S | |
| 54 | **Save storage: local-first write-through + divergence-safe sync.** Guest/cloud routing is decided per call by a live auth check, so silent auth expiry forks a game between IndexedDB and the cloud invisibly (2026-07-04 playtest: the goblin-campaign save "teleported" between login states). Keep the local fallback (never lose progress) but make it honest and self-healing: write-through local-first saves, merged saved-games list with sync badges, reconcile on auth restoration, and rev-counter fork-on-conflict for multi-device divergence. Design: `docs/SAVE_SYNC_PLAN.md`. Phase 3 gated on the Hetzner cutover. **Phase 1 shipped 2026-07-04 (pending review)**: honest fallback via `pending_cloud_sync` row stamp + `storage` marker on `conversationsApi.save/updateMessages` + `savedLocal` save status/modal copy; `lastKnownAuth`/`hasSeenSignedIn` auth-check hardening (`resolveRoute()`); timestamp-guarded `runLocalGameSyncPass` that re-runs on `SIGNED_IN`/`TOKEN_REFRESHED` and parks diverged local copies ("diverged on this device") instead of clobbering newer cloud rows. **Phase 2 shipped 2026-07-05** (write-through + merged list, see #57). **Phase 3 shipped 2026-07-05 (pending review + manual migration)**: `rev` lineage counter (migration 003; the maintainer applies it via psql BEFORE deploying the worker) + revision-guarded upsert with a 409 `rev_conflict` contract (current rev + updated_at); client `base_rev` tracking (set on load-from-cloud and on every push; legacy rows stay unconditional, so divergence is never invented); on 409 the local timeline is parked ("diverged on this device"), the cloud row adopted, the live session redirected to the parked copy (one fork = one copy), and `performSave` reports `'forked'` with honest save-modal copy; hero-ledger union across the diverged twins (dedupe by event identity, rollup-safe refusal) writes into the adopted row so the losing timeline's XP/gold/items are raised back by the existing load-time reconcile; `runLocalGameSyncPass` prefers rev lineage over timestamps when both sides carry it. Express dev server mirrors the contract; harness `scripts/test-cf-rev.mjs` (manual). | Playtest 2026-07-04 | M (Phase 1: S) | Phases 1-3 done (pending review; migration 003 not yet applied) |
| 55 | ~~**2026-07-04 t2 playtest fixes.**~~ **DONE 2026-07-04.** (a) Milestone/quest XP now party-wide: `applyPartyRewardsToAll` gives FULL XP to every member (authored values are per-hero-curve-tuned; splitting would strand non-leads), loot still pools via lead; previously ALL milestone+quest XP went to hero #1 only. (b) t1-to-t2 bridge honesty: continuation picker computes `openingAccessible` and tells under-leveled parties the opening is within reach + rumours bridge to the gate, instead of only "may be deadly". (c) "Harbormaster/Stablemaster of undefined": civic buildings are never named by the generator; NPC jobs now fall back to the town name (new towns) + BuildingModal strips the stale tail (cached towns). (d) "Leader of Millhaven" in the archives: retro house-swap now rehomes displaced cached residents (the Elder lives in the first-scan house, exactly the tile the swap picks). Gender distribution audited: 50.2% F over 9k NPCs, no bias. | Playtest 2026-07-04 | S-M | Done |
| 56 | ~~**Explorable-site content wiring (2026-07-04 playtest R1-R6 + site audit).**~~ **DONE 2026-07-04.** (a) In-modal feedback: new `siteNotice` channel (useGameMap → SiteMapDisplay) mirrors every site grant (loot, objective item, location arrival, quest-step/milestone completion) that previously only reached the chat log hidden behind the fullscreen map modal; that invisibility was the actual root cause of "can't harvest crystals" (R6): the grant worked, the player just never saw it. (b) R1 "Sealed Vault gave nothing": `injectSiteObjective` no longer deletes the deep hoard; the objective CARRIES the slot's loot and Game.js pays it out on arrival for item/location objectives. (c) Multi-quest-per-site: `getActiveSiteObjectives` returns ALL active objectives per site type and injection places each in a distinct slot (deepest-first, item > combat > location on overflow), idempotently per milestoneId on every entry, so a second active cave quest (cursed_patient) actually spawns its item. (d) Harvest nodes: caves grow mushroom/fungi/gem/ore nodes (3+ each per cave, covering every gather-3 quest in one site), mountains keep crystals + gain ore, ruins gain urns + overgrowth relic caches (R2 "nothing to pick up in ruins"); leftover harvestable-kind decorations are demoted (no teasers); cached sites untouched (`populated` guard). (e) R3: pools render as programmatic SVG water tile art (siteTileArt `ART_POI`), not a 💧 emoji. (f) Gather quests carry `sites` source hints honored by reveal (`getRevealedSiteTypes`), eligibility (`isQuestEligible` + launcher/derive availability now include forest/hills/mountain), quest-log hints and the DM prompt; `cave_entrance` added to encounter POI mapping so cave-themed world encounters fire. (g) Side-quest XP payouts (`rewardMessages`) surfaced in the completion system lines + site notice. Deferred: hoard-carry payout for COMBAT objectives (boss keeps its own rewards; carried loot is attached to the content but not granted), forest/hills harvest nodes, and node respawn/economy tuning (nodes are one-shot per site; storm_crystal x3 may take 2-3 mountain sites). | Playtest 2026-07-04 + site audit | M | Done |
| 57 | ~~**Save-sync Phase 2: local-first write-through + merged list.**~~ **DONE 2026-07-05.** Every live-session save writes IndexedDB first (synced flag), then pushes cloud and marks synced (guarded against mid-push saves); loads take the newer of the two copies; saved-games list is a cross-store union with a "💾 On this device" badge; failed cloud pushes return honest savedLocal and auto-reconcile via PENDING_LOCAL_SAVE_EVENT + app-start pass; live session keeps its write-ahead copy, synced non-live local rows prune. Ends the twin-copy flakiness class (items/XP "wiped" by stale-twin loads). Phase 3 (rev protocol) remains, gated on Hetzner cutover. | Playtest 2026-07-04 | M | Done |
| 58 | ~~**Hero invariants + append-only grant ledger (SAVE_SYNC_PLAN section 9).**~~ **DONE 2026-07-05.** heroInvariants.js: on-load checker heals upward (level from XP, maxHP from formula, HP clamp, dangling equipment cleared + reported, negative floors) with one visible "🛡️ Restored:" line; never breaks load. heroLedger.js: every XP/gold/item grant appends an immutable event in settings.heroLedger (cap 400, rollup baselines preserve sums); gold spends ledgered as negatives; snapshot regressions below ledger sums are raised back. Ledger-union merge across diverged twins deferred to Phase 3 (shipped there 2026-07-05, see #54). | Maintainer direction 2026-07-04 | M | Done |
| 59 | ~~**World-gen: twin giant lakes dominate the map.**~~ **DONE 2026-07-05.** Lake water budgeted to 12% of tiles (12 on 10x10), max ONE large lake (a second only spawns at <= half the first's actual tile count, min 3), and per-lake shape personality (seeded axis-stretch + wander) so two lakes never grow as copies; 300-seed survey test pins the caps, size rule, >= 2-tile separation, count distribution (never 3+), and town placement (pre-fix 15 of 47 two-lake maps were twin giants >= 6 tiles each; post-fix 0). Going-forward-only; existing saves keep their grids. | Playtest 2026-07-05 | S-M | Done |
| 60 | **Larger world maps for paid tiers (design first, no code).** Guests/free stay 10x10. Paid tiers get bigger worlds (already on the tier ladder under Premium). Consequences to design before building: (a) DISPLAY: larger grids need scroll and probably zoom in/out in MapModal/WorldMapDisplay; (b) QUESTS: do not break current quest/milestone authoring, and milestones must not spread too far apart; (c) GENERATION: build very large maps in CHUNKS with continuity at chunk boundaries (biome/river/road stitching); keep t1+t2 campaign content broadly within the same chunk (refine later if needed); (d) BC: mapVersion stamping + going-forward-only generation, per the map compatibility rules; sub-map caches and pathfinding perf at larger sizes need a look. Design: `docs/LARGER_WORLDS_PLAN.md` (agreed 2026-07-05). | Maintainer 2026-07-05 | L (design done) | Design done |
| 61 | **Large-world chunk-assembly prototype (plan step 3) — DONE, debug-only.** `src/utils/worldAssembler.js` assembles kxk chunk worlds behind `/debug/large-world`: heart chunk via the untouched legacy `generateMapData` path (byte-identical, fixture-pinned), deterministic `chunkSeed(worldSeed,cx,cy)` for outer chunks, ocean-side model (the heart's coast edge defines the WORLD's ocean side: the coastline continues along it with ±1 depth wobble, beyond it is open ocean, other sides are coast-free), one deterministic `gatePoint` road crossing per land-land seam reusing the existing `hasPath`/`pathConnections`/`pathDirection` representation (no new tile field), outer-chunk lakes keep 2 tiles off seams (no bisected lakes), ~65% settlement density in the outer wilds, customNames resolve heart-only. `generateMapData` gained an additive `options` param (suppress/prescribe coast, edge constraints, lake margin, town density); legacy calls are fixture-pinned byte-identical. Seed/gate scheme is EXPERIMENTAL and NOT frozen (plan §3/§7a: freeze only when saves depend on it). **Step 2 SHIPPED 2026-07-05 (dark):** world-map viewport with three fixed zoom steps + tile culling above 400 tiles; worlds of 10x10 or smaller take the legacy byte-identical branch (9fad156). **Remains:** entitlement gate + New Game world-size option (step 4, needs #39/#40), growable saves + mapVersion machinery (step 5); nice-to-haves: occasional ocean islands, seam-aware mountain-range naming, river continuation across seams. | Plan step 3, 2026-07-05 | M (prototype done) | Done (debug-only) |
| 62 | ~~**Town path redesign: hub-and-spoke with windy spokes.**~~ **DONE 2026-07-05 (pending review).** Replaced the old piecemeal path system (straight L-shaped `createPath` rays from each building to the nearest road, which skipped blocked tiles and left disconnected fragments — 191/200 surveyed towns had multiple path components, 757 singleton orphan tiles) with a hub-and-spoke network in `townMapGenerator.js`: the town square is the hub; every gate (seeded jitter along its edge) routes to it via a windy lane (seeded per-tile cost-noise Dijkstra, `PATH_WINDINESS`, plus a perpendicular midpoint waypoint so axis-aligned gate roads can't collapse to straight rays); major buildings (townhall/tavern/inn/temple/market/blacksmith + keep) get hub spokes, everything else stubs to the NEAREST lane; buildings connect the moment they are placed so later placements can never seal one off; houses are placed AFTER the lanes and prefer street-front tiles; waterfront stubs run along the shore with dock sand protected from paving; `pruneOrphanPaths` guarantees zero orphans. Survey (580 towns incl. water/river): 1 connected component always, all gates + keep connected, every non-house building (rare estate-courtyard manors aside) fronts a lane, >= 5 free grass tiles near buildings for quest injection. Tests: seed survey + windiness floors + water/river invariants in `townMapGenerator.test.js`; `/debug/tileset` gained a path overlay + connectivity/windiness stats. Going-forward-only; cached town maps keep their old layouts (tile art unchanged, no new tile fields). | Maintainer 2026-07-05 | M | Done (pending review) |
| 63 | ~~**World-level lake quota for chunked worlds.**~~ **DONE 2026-07-05.** Independent 32% per-chunk rolls compounded to 5-lake 3x3 worlds; outer land chunks are now ranked by their seeded roll and lakes granted from a WORLD budget (`WORLD_LAKE_CAP = 3`, heart's own lakes included), reserved in LAKES not chunks (a granted chunk may roll a second smaller lake, so each grant debits its `maxLakes`). Deterministic for fixed world dimensions; 30-seed 3x3 survey test pins never more than 3 total lakes. | Playtest 2026-07-05 | XS-S | Done |
| 64 | ~~**Themed town tilesets (snow/desert).**~~ **Tier 1 DONE 2026-07-05** (building material palettes: adobe/terracotta desert, dark-timber/snow-capped snow; themed legend swatches; byte-identical temperate pins; grounds had already shipped). Tier 2 DONE 2026-07-06: desert flat roofs (parapet + awning on house/shop/market), snow chimney smoke + icicle fringe on dwellings; civic silhouettes pinned unchanged. Towns render temperate everywhere; world art already themes forests/hills per biome. Tier 1: palette + ground swap in townTileArt keyed off town theme (snow ground/roof caps; sand/adobe), going-forward (cached towns lack theme -> temperate fallback). Tier 2: architecture flavor (flat roofs/awnings vs steep roofs/smoke). Strengthens premium biome worlds. | Design chat 2026-07-05 | S (tier 1) / M (tier 2) | Done (tiers 1+2) |
| 65 | **Water towns: canal city (Venice) + river city (Konigsberg) — PREMIUM feature.** Eligibility tied to an estuary (coastal tile where a river meets the sea; world-gen nearly produces this today). Canal city: canals AS the path network (water spokes on the new hub-and-spoke system, quay lanes, bridges); river city: river forks around an island district, bridges knit it. Needs largest grid or reduced density (canals eat buildable space; preserve quest-injection grass supply). Pairs with the tier ladder's sea maps + ships. Design before code. **Design done: `WATER_TOWNS_PLAN.md`** (2026-07-05). **Phase 1 built 2026-07-05 (debug-only, pending review):** river-city fork carve (`water.archetype: 'riverfork'`, town + city), additive `tile.waterway` field, WATERWAY_BRIDGE_COST crossings, island venue bias (temple + archives on cities, market on towns), >= 2 guaranteed bridge crossings, >= 4 reserved island quest-grass tiles, seed-survey invariant tests + legacy fixture pins, river-city preset on `/debug/tileset`. **Phase 2 built 2026-07-05 (debug-only, pending review):** canal city (`water.archetype: 'canal'`, CITY size + coastal context only, everything else falls back to plain generation): 2x2 harbour basin off the square (never bridged), guaranteed basin-to-sea channel + seeded second outlet (fences off districts) + 2-4 interior spokes (3-5 total, water budget unified with the sea), CANAL_BANK_COST quay pull (opt-in `bankCost`; non-canal routing byte-identical, coastal-city fixture pin), CANAL_BUILDING_CONFIG density variant (houses 55 -> 42, stables/mill dropped), reserved quest-grass floors (10 core + 4 per district), new `boathouse` building type moored on a bank (townTileArt shape + legend + emoji, Boatwright staffing in npcGenerator), 40-seed x 4-coast survey tests, canal-city preset + stats + overlays on `/debug/tileset`. **Phase 4 (art) built 2026-07-05 (pending review):** 16-mask directional canal tiles (`canalTile` in `townTileArt.js`: banks/bends/T/cross/mouths on calmer darker water), mask-derived basin treatment (3-4 wet neighbours = open water + corner nibs), bridge-over-canal (deck oriented across the channel, water visible beneath; plain bridges byte-identical), quay lips on paths beside waterway water, themed bank masonry (stone/sandstone/ice), `waterwayMask` wiring through `TownMapDisplay`/`tileBackground` (additive arg; sea/lake water and non-canal paths pin-tested byte-identical), Canal legend swatch + vocabulary gallery on `/debug/tileset`; fork water re-skins for free via the shared `waterway` flag. **Phase 3 (world integration) built 2026-07-05 (pending review):** additive `generateMapData` options `riverToSea` (first river targets the nearest non-lake water; 60/60 surveyed seeds reach the coast vs 34/60 legacy) + `estuaryTown` (a normal-budget town placed at the river's sea mouth, pinned to CITY size, never the starting town), both default-off and fixture-pin byte-identical; `src/game/waterTowns.js` stamps `worldTile.waterTown: 'canal' | 'riverfork'` ONCE in `launchCampaign` through `canUseWaterTown` (canal: premium, max 1 per world, first non-starting coastal river city with a pure-coast lagoon fallback: 150/150 placement, 88.7% true estuary; riverfork: member+, seeded ~50% roll over eligible river town/city settlements, measured 54.2%); saves grandfather the stamps (tier checked once, never re-checked); `townWater.getTownWaterContext` carries the stamp into town generation as `water.archetype` (launcher pre-generation + all three `useGameMap` lazy call sites; cached towns untouched); tier-gated New Game note (absent for free tier, decision 13) and the manual map preview uses the same options; quest-injection integration tests inside generated canal/riverfork towns. **Remains: Phase 5** (packaging polish: flagship canal template in `premiumTemplates.local.js`, locked NewGame card) and Phase 6 live-save verification. **Phase 6 built 2026-07-06:** six water-flavored side quests (smuggling bust, ferry grievance, harbor fees, quayside cargo, boatwright resin gather with forest tap nodes, pier vermin cull), gated by water-venue presence so landlocked worlds never see them. | Maintainer 2026-07-05 | L (design: S-M) | Design done; Phases 1-4 + 6 built; Phase 5 remains |
| 66 | **Realistic world maps (direction note).** Generate world-level FIELDS (elevation/moisture) and derive features from them: rivers downhill to the sea, lakes in basins, biomes by moisture, towns on rivers/coasts, roads through valleys. Dovetails with #60: fields generated at full world extent + chunks sampling them makes seam continuity free (edge constraints become a transition step). Prior art: TERRAIN_ROADMAP.md noise-terrain experiment (debug-only). Phase: hydrology first (rivers reach the sea -> estuaries, feeds #65), then settlement rules, then elevation-first generation. | Maintainer 2026-07-05 | XL (phased) | |
| 67 | ~~**Water-adjacent settlement placement bias.**~~ **DONE 2026-07-05** (river doctrine wave, 60dc650). Sizes are dealt before placement so bias scales per size (city 0.8 .. hamlet 0.2, never 1.0); river-band tiles weigh double with an extra pull for town/city slots. Survey: water adjacency 79% -> 89%, dry inland towns still occur; member-world riverfork eligibility 87.5% -> 90.8% of worlds with +17% eligible settlements. Universal across tiers (the premium boundary is the archetype, not the river), going-forward-only; legacy world fixtures deliberately re-captured (chunk scheme still experimental). Doctrine: WATER_TOWNS_PLAN 1c. | Design session 2026-07-06 | S-M | Done |
| 68 | ~~**Riverside (edge) river towns.**~~ **DONE 2026-07-05** (river doctrine wave, 60dc650). A town ADJACENT to a river now gets a 'riverside' waterway band along that edge (flowing off both ends and the far side), a quay lane wall-to-wall on walled sizes (also fixed sealed house pockets found in survey), and an optional far-bank strip with at most one seeded bridge on small sizes, near-uncrossable otherwise. Water-context precedence documented: riverside < lake < coast < on-tile river < archetype stamp; mills prefer the bank in riverside settlements (3-in-4 seeded). Doctrine: WATER_TOWNS_PLAN 1c. | Design session 2026-07-06 | M | Done |
| 69 | ~~**Riverfork Phase 3.1: coastal forks fall back to the plain band.**~~ **DONE 2026-07-05** (river doctrine wave, 60dc650). A flooded flow-axis end now merges the channel into the sea instead of degrading to the plain band (80/80 survey cases fork; inland/away cases untouched). The same wave opened river mouths at the map border (waterway border tiles no longer render a bank wall damming the flow) and added bridge-cost tuning so routes funnel over built crossings (awkward crossings -49% canal / -51% fork). | River-fix agent 2026-07-06 | S-M | Done |
| 70 | ~~**bell_of_the_last_tide catalog entry.**~~ The Drowned Bells flagship (private repo, tidewater-t3) rewards a new item id: proposed very_rare artifact +3, value 8000 (crown_of_sunfire rung). Needs the ITEM_CATALOG entry + icon (art queue) BEFORE the campaign is loaded into premium_templates, or the finale reward dangles. Also flagged: per-template minTier so member-wide canUseTemplate can gate premium-only templates once #40 delivers. | Flagship authoring 2026-07-06 | XS-S | Done 2026-07-06 (icon borrows ancient_artifact until the queued bespoke art lands) |
| 71 | ~~**Worker test framework.**~~ **DONE 2026-07-06.** @cloudflare/vitest-pool-workers suite in cf-worker/test: 78 always-on tests (auth incl. real ES256 JWT verification against a stubbed JWKS, schema bounds, model fallbacks, sanitization, rev-protocol 409s, tier-laddered template delivery) + 38 branch-gated premium tests that activate when #52 merges (caps 32k/1500/800, fail-open limiter, premium gate contracts, model-choice-ignored). Fully offline, zero credentials, zero worker-source changes, ~2s. `npm run test:worker`. | Maintainer 2026-07-06 ("are we sure these limits are working?") | M | Done |
| 72 | ~~**Tiered New Game discovery (t2/t3 sections on the picker).**~~ **DONE 2026-07-06 (pending review).** New Game listed ONLY tier-1 starters, so tier-2/3 campaigns (including server-delivered flagships like The Drowned Bells) were invisible until the continue-legend picker offered them; a premium subscriber could not even see that higher-tier content existed. Maintainer: "we don't open up t2 quest cards on the game page, but we probably should. Then t3 as appropriate." Shipped: Ready-Made tab gains "🛡️ Seasoned Parties (Tier 2)" and "🐲 Legendary Campaigns (Tier 3)" sections (`src/game/templateSections.js` grouping; comingSoon stubs excluded from the higher-tier sections so Legendary hides until real t3 content registers; same card component, tier badge already carries the Lv band); card/modal/submit gating moved from the coarse isTemplatePremium check to `canUseTemplate` so per-template minTier (premium-only flagships, #70) locks correctly; selecting an out-of-band t2/t3 shows the continue-picker-class honest note (opening within reach vs level-gated/brutal, via shared `isOpeningAccessible` in campaignChain.js), never a block; HeroSelection's level banner uses the same copy pattern (`getLevelFitNotice`) and Start stays enabled (engine minLevel gates protect deep milestones). t2/t3 from New Game launch as fresh worlds through the unchanged `launchCampaign`; runtime-registered templates appear without reload via the existing premiumContentReady re-render. Tests: section grouping + delivered-template re-filter, higher-tier card gating, warning triggers, real t2/t3 launches incl. a runtime-registered t3 with its own customNames. **Follow-up (2026-07-06, entitlement-filtering pass):** Custom/Freeform content sources now filter by real entitlements at the source helper, not in JSX: `getCustomQuestItems(tier)` (questPickerData.js) excludes the flagship/unobtainable reward set (bell_of_the_last_tide, the t3 legendary shelf) for every account and caps item rarity by campaign tier (mirrors maxRarityRankForTier; tier 3 coerced to the t2 ceiling for non-members); NewGame uses it for the slot-1 dropdown and all lookups, coerces stale slot/theme state, and re-checks the premium worldTheme carryover whenever tier state settles. Audit confirmed buildings, NPC roles, POIs, theme name pools, and quest enemies carry no premium entries today (premium biome ids resolve to zero enemies), pinned by tests in questPickerData.test.js. | Maintainer 2026-07-06 | S-M | Done (pending review) |
| 73 | **Arc cards + overarching narrative (New Game consolidation).** The Ready-Made tab is overwhelming (~16-19 tier cards across five sections after the tidewater stubs); consolidate to 7 campaign-arc cards (one per theme, derived from the live storyTemplates by theme+tier) with a chapter ladder and per-chapter lock chips, absorb the Seasoned/Legendary sections into a per-arc chapter picker in the detail modal, and retrofit a shared meta-narrative binding all 7 arcs (framing copy only, no authored campaign rewritten; future capstone campaign reserved, private repo). Presentation-only, zero save impact; the in-save chain (campaignChain.js) already IS the arc mechanically. Design: `docs/ARC_CARDS_AND_NARRATIVE_PLAN.md` (decisions 1-4 approved, framing B chosen). **Phase 1 shipped pending review 2026-07-07**: `storyArcs.js` derivation + 7 arc cards grouped by entry gate, `ArcDetailModal` chapter picker (absorbs Seasoned/Legendary, stub-tolerant: fixes the guest teaser-preview crash), teaser self-heal via `retryPremiumTemplates()`, free heroic finale stub (`minTier: 'free'`) + stub-replaces-comingSoon merge. Phases 2-4 (authored taglines/art, meta-narrative copy, capstone) remain. | Maintainer 2026-07-07 | M (phase 1) / L (full) | Phase 1 shipped, pending review |
| 76 | **AI narration contract: "engine referees, LLM narrates" (decided 2026-07-15, not yet scheduled).** All game outcomes including milestone completion become engine-decided; the LLM is told what happened and narrates it, never judges it. Kills the premature-marker misfire class surfaced by the 2026-07-15 model trial (every candidate, incumbent included, emitted premature `[COMPLETE_MILESTONE]` markers; the best-value candidate's only blemish was 4 of them). Work implied: DM_PROTOCOL rewrite (drop the marker/judgment blocks, go few-shot with explicit length targets), strip completion markers in both `sanitizeResponse` passes, remove client marker parsing (`MILESTONE_COMPLETE_REGEX`/`CAMPAIGN_COMPLETE_REGEX`/`resolveTalkMarkerMilestone`), append "What do you do?" in code, convert NewGame's custom `type: 'narrative'` milestone to `talk`, engine-derived campaign completion, scripted text for quest completions + deaths (openings already authored, 2026-07-07), renderer-mode model re-eval (`scripts/eval-premium-models.mjs --mode=renderer`, in progress). Open: legacy saves' narrative milestones, scripted-beat order, exact length target, talk-narration special casing. Design: [AI_NARRATION_CONTRACT.md](AI_NARRATION_CONTRACT.md). | Maintainer 2026-07-15 | M | Decided (design done), not scheduled |
| 77 | **AI-authored quests/campaigns with deterministic milestones (direction note, 2026-07-16).** Lean harder into AI as a *content author* for campaigns while keeping the *mechanics* fully engine-refereed. What exists today: `handleAiGenerateStory` (NewGame Freeform tab, `src/pages/NewGame.js`) does a single **zero-shot** prompt → JSON (shortDescription, campaignGoal, 3 milestones as `{text, location}`, tone dials, customNames). Untested recently; needs a re-check. The gap: those AI milestones are **untyped freeform** — they never get the deterministic engine wiring authored templates carry (typed `item`/`combat`/`talk`/`location` triggers, unique quest-item ids absent from loot tables, placed milestone NPCs, boss `dealsDamage`/`damage` profiles, POI sprite + `POI_IMAGES` art), so they can't complete deterministically the way `storyTemplates.js` campaigns do. Direction: (a) AI writes the **prose** (descriptions, milestone/quest flavor, NPC lines) but the **structure is scaffolded deterministically** and validated — the "ready-made quests, mostly pre-written" model where AI fills authored slots, not invents mechanics; (b) **no zero-shot** — use few-shot / multi-step prompting (propose beats → map each to an engine-valid milestone type + venue → generate text → validate), and run the output through the same content-integrity gate as hand-authored campaigns (`campaign-author` skill / `npm run audit`, plus the `storyTemplates.js` authoring invariants: unique quest-item ids, every POI has art, `requires` gating). Possible extension: quests generated **dynamically in-world** (beyond New Game), still emitting engine-typed milestones. Tightly coupled to #76 (engine referees, LLM narrates) — same "AI narrates, engine decides" contract. Entitlement-gate generation cost per #39/#72. **Phase 0 shipped 2026-07-16:** the Freeform "Generate with AI" button (it lives on the Freeform tab, not Custom) is now Members+ (`aiGenLocked = !user || !premiumUnlocked` in `NewGame.js`, greyed with a "🔒 Members feature" prompt for free/guests; handler fails closed). Design: [AI_QUEST_GENERATION_PLAN.md](AI_QUEST_GENERATION_PLAN.md). | Maintainer 2026-07-16 | L (design first) | Phase 0 (Members gate) shipped; design done, not scheduled |
| 78 | **Game feel: audio + visual juice (brainstorm, 2026-07-16).** Make the game *pop* without touching mechanics, as a pure presentation layer (same discipline as tile art / the #76 narration contract: effects react to engine events, never change outcomes). Seeded from a "game juice" YouTube talk; living idea list. Four buckets, each assessed for a turn-based RPG: **Music** (ambient/town/combat/boss/victory themes — headline **Members+** benefit), **Reactive SFX** (fits *well* because turn-based events are discrete: dice roll, per-outcome stingers, level-up, milestone, loot, modal — base polish, mostly free), **Reactive particles** (selective/light only: level-up sparkle, floating "+XP/+gold" numbers, rare-loot shimmer; full particle systems are action-game juice, skip), **Reactive glow/border on buttons** (cheap CSS, high value: pulse the actionable button, glow the Talk button when a milestone NPC is present, red danger border in boss fights, low-HP vignette). **Baseline: zero audio in the codebase today (greenfield); ~12 CSS keyframe blocks already exist.** Doc maps every game surface (world/town nav, encounter modal, progression/rewards, modals/buttons, high-stakes beats) to the effects that fit, plus cross-cutting reqs (master mute + Music/SFX volume in SettingsContext, browser autoplay-gesture policy, `prefers-reduced-motion`, lazy-loaded/tier-gated audio assets, determinism boundary). Phasing: (1) free CSS button glow/danger states, (2) SFX manager + core event sounds, (3) light particles, (4) premium music system. Open: exact free/premium split, audio sourcing/budget, self-hosted audio manager vs library. Design: [GAME_FEEL_PLAN.md](GAME_FEEL_PLAN.md). | Maintainer 2026-07-16 | L (phased) | Idea captured, not scheduled |
| 79 | **Combat UX: animations + modal refine + open-play rework (brainstorm, 2026-07-16).** Three related presentation threads (mechanics unchanged, owned by ENCOUNTER_SYSTEM.md; same determinism boundary as #78/#76): (1) **combat animations** (the combat slice of #78 — dice roll-up, outcome pop/shake, floating damage numbers, HP-bar impact pulses; `encounters.css` has almost no animation today, just a spinner + width transitions); (2) **refine the clunky modal**; (3) **rework away from modals to inline "open play", more TTRPG-like.** Current state: combat is a two-modal chain (`encounterInfo` arrival gateway → the **~1440-line** `EncounterActionModal` fight). The engine (`encounterResolver`/`multiRoundEncounter`/`encounterController`) is pure/deterministic, BUT the fight *flow* (phase machine: formation→initiative→action→resolving→round→final) is trapped in the modal as ~15 state hooks + nested ternaries. Clunk: mega-component, modal open/close churn (`encounter closes navigation` forces `reopenMapAfterEncounterRef` at 7+ Game.js sites + `setTimeout` FocusTrap hacks), latency-theater spinner (async but fully local), item-picker-modal-in-a-modal (raw zIndex 4000), heavy clicks-per-round, dead `EncounterActionModalSimple.js`. We are already TTRPG-shaped mechanically (d20 + Lead+Support + multi-round); "open play" is a *presentation* change (inline in `GameMainPanel` vs fullscreen overlay), not a rules change. **Keystone:** extract flow into a headless `useEncounterFight` hook/reducer — behavior-preserving, and it unlocks all three threads. Recommended phasing: refactor first → refine+animate (single persistent combat panel, kill spinner/interstitials) → spike open-play behind a debug route → decide. Design: [COMBAT_UX_PLAN.md](COMBAT_UX_PLAN.md). | Maintainer 2026-07-16 | L (phased) | Idea captured, not scheduled |
| 80 | **Mood theming: grimdark & eldritch map visuals (brainstorm, 2026-07-16).** Give the **grimdark-survival** and **eldritch-horror** campaigns a visual identity on the map (darker tiles, plague/decay signs, evil spirits/wrongness) the way desert/snow already re-skin the world. Today those genres render on **default temperate tiles**. Key distinction: desert/snow are *biome* themes (`PREMIUM_THEMES`, palette swap in `townTileArt`/`worldTileArt`); grimdark/eldritch are *moods* in `THEME_DEFAULTS` (orthogonal to biome, currently free genres, no tile art). So the mechanism is a **mood layer applied on top of any biome** (desaturate/darken + decay/spirit motifs), not a new biome. Retroactive hook: key off the `grimnessLevel`/`darknessLevel` dials already on every save (grimdark = Grim/Dark, eldritch = Bleak/Dark) → re-skins existing horror saves with no migration (art is a pure view, BC-safe). Two distinct flavors: grimdark 💀 (muted/grimy, dead trees, gallows, plague pits, ravens, blood accents) vs eldritch 🐙 (sickly green/violet, warped trees, runes, spectral wisps, miasma). Two levers: (A) per-tile baked dressing extending the #64 tile-art seam (cheap, retroactive) and (B) a NEW ambient overlay layer over WorldMap/TownMapDisplay (vignette/fog/animated wisps — none exists today; shared with #78's ambient work). Gating tension (open): biome tilesets are Members+ but these genres are free, so proposed base mood dressing free + animated overlay premium. Preview on `/debug/world-map-art` + `/debug/tileset`. Design: [MOOD_THEMING_PLAN.md](MOOD_THEMING_PLAN.md). | Maintainer 2026-07-16 | M-L (phased) | Idea captured, not scheduled |
| 81 | **TTS: dynamic voice narration for the DM (research done, 2026-07-16).** Speak the AI narration aloud. Fits the stack well and maps cleanly onto the existing deterministic-vs-LLM split. **Recommended start (Phase 1): Workers AI Deepgram Aura (`@cf/deepgram/aura-1`) called from the CF Worker, fronted by an R2 cache keyed by `hash(text + voice)`** — a one-binding extension of the existing Worker (no GPU; do NOT run a TTS model inside the Worker isolate: 128MB/ephemeral = cold-load OOM). Aura returns a ReadableStream (+ WebSocket) so LLM tokens can stream into TTS and audio streams out. **Caching is the key synergy:** deterministic/repeatable text (combat resolutions, item descriptions, UI barks) → high hit rate → synthesize once to R2 (no egress fees) or pre-bake offline; only unique LLM narration pays on-the-fly cost/latency. **Phase 2:** move *dynamic* synthesis client-side (`kokoro-js` / `onnxruntime-web`, quantized, chunk-by-sentence + streamed playback in a Web Worker) for zero backend cost + open-model control; keep R2 deterministic lines. **Reserve:** self-host Kokoro/Supertonic ONNX on Hetzner for batch pre-gen/total control (CPU competes with the DB). Open-model candidates: Kokoro-82M (most natural, English-only, Apache-2.0) vs Supertonic-3 (faster/lighter, 31 langs). **Character/NPC voices** (cloning + emotion: Chatterbox/Orpheus/NeuTTS Air) don't fit the current stack directly → need a GPU host OR offline pre-gen of a fixed NPC line set cached to R2 (folds into the same caching split). **Pre-recorded authored text (Phase 1b):** a growing share of the game's text is authored+fixed (campaign openings, #76 scripted beats, #77 ready-made quest intros/turn-ins, boss/NPC lines), so it can be voiced ONCE offline at author/build time into the *same* R2 hash-cache (keyed by stable id + text hash, regenerated only on text change) and never pay runtime cost — using the best/heaviest models or even **human voice actors** for flagship/premium campaigns (a differentiator dynamic TTS can't match). Two-track mix: authored text → pre-recorded static assets (highest quality, zero runtime cost), unique LLM narration → on-the-fly TTS (Aura/kokoro, R2-cached). Strategic alignment: the more content leans pre-written (#76/#77), the larger the pre-recorded share and the smaller the runtime-TTS bill. **Voice architecture (decide FIRST):** consistency is required *per voice role* (same model+voice+params) — the DM narrator interleaves authored+dynamic every turn so it's locked to ONE affordable runtime voice and can never be human-VO'd; only *entirely-authored* roles (opening cinematics, boss intros, fully-authored NPCs) can use a different premium/human voice. Drama reality: the cheap on-device models (Kokoro/Supertonic/Aura/Piper) are neutral audiobook-grade, NOT dramatic; real drama needs Orpheus/CosyVoice/Chatterbox (GPU) or ElevenLabs/Hume (managed) or human VO — affordable only on the finite pre-recorded set-pieces, which is exactly where the dramatic moments are. Net: neutral consistent narrator for dynamic play + premium dramatic voice on pre-recorded authored cinematics.
| 83 | **Skill checks for narrative actions: engine-rolled, BG3-style (proposed, HIGH PRIORITY, 2026-07-16).** Social/narrative actions (persuade, intimidate, sneak, perceive) currently have NO mechanics: the LLM simply decides whether they work, the one surface where the player can argue the model into success and where character sheets don't matter. Target: at consequential moments the game declares a check, the ENGINE rolls d20 vs a `DIFFICULTY_DC` tier (model proposes `[CHECK: skill, tier]`, bounded judgment; authored surfaces declare their own), and the LLM narrates the result it is handed as fact, extending the #76 contract to social play (resolves its open question 5: the `[CHECK/ROLL]` trigger stays, upgraded). Today's `[CHECK:]` path is a DEAD END: `TRIGGER_REGEX` → `checkRequest` → DiceRoller opens, but no `onRollComplete`, no DC, no consequence, result never returns to the AI; DiceRoller also skips the level/gear/support modifier stack combat gets. Core design rule: **failure must change world state, never just block** (fail-forward costs via gold/`affectedFactions`/favor quests, complications via the side-quest hook path, per-(NPC, topic) lockout persisted in a check ledger + injected into the prompt to kill retry-spam, crit-fail escalation into real encounters); hard constraint: a failed check never dead-ends the golden path (roll decides the *price* of progress, never its *possibility*). Phase 1 (close the loop + modifier parity + result-as-fact narration) must ride the #76 DM_PROTOCOL rewrite. Design: [SKILL_CHECK_PLAN.md](SKILL_CHECK_PLAN.md). | YouTube comment + maintainer 2026-07-16 | M-L (phased) | Proposed, HIGH PRIORITY, not scheduled |
| 82 | **Landing page + UI redesign (proposed, HIGH PRIORITY, 2026-07-16).** Maintainer: "arguably the most important" item. The site is functional and clean but visibly behind competitors — and this is a **known competitor-analysis loss** (site-presentation gap, flagged cheap-to-close relative to multiplayer/combat-fidelity/mobile). Strategic, not cosmetic: our differentiation is *credibility* (provable/auditable determinism, structured generated campaigns), and a weak site undercuts a credibility pitch specifically — a polished site makes the honesty wedge read as a product choice, not a resource constraint. Current state: `HomePage.js` is an **emoji nav-card dashboard** (title + one-line subtitle + ⚔️/📚/🧙‍♂️ cards), i.e. an app launcher with **zero marketing/conversion content**, no hero art, no differentiation messaging, no screenshots/prose samples/illustrated characters; it also **conflates first-visit and returning-player** (same grid for both). The design system is a fine foundation (Cinzel/Lora/Inter, parchment+gold/purple themes, CSS-var tokens in `index.css`) — the gaps are layout, content, art, consistency, and the missing marketing surface. Three tracks: **(A) a real logged-out landing/marketing page** (hero art + wedge headline, proof-of-determinism, show-don't-tell screenshots/prose/character lineup, differentiators, pricing/CTA) — highest ROI, self-contained, do FIRST; **(B) in-app UI polish** (split the returning-player dashboard out, consistency pass on game UI/typography/buttons/HUD, finish the ModalContext migration, dovetails #78/#79); **(C) design-system consolidation** + optional brand refresh. Responsive/mobile is table stakes (competitors are mobile-first). Presentation bar: Chronicles of Terros. Design (business-sensitive, local-only): `docs/private/UI_REDESIGN_PLAN.md`; positioning source `COMPETITORS.md` §5. | Maintainer 2026-07-16 | L (phased; Track A: M) | Proposed, HIGH PRIORITY, not scheduled | Latency trick throughout: chunk by sentence, stream so time-to-first-audio is the short-line number. Likely a Members+ sensory feature given per-use cost (gating open, follows #78/#80 split). Pairs with the audio work in #78 (voiced narration alongside music/SFX). Notes: [dungeongpt-tts-notes.md](dungeongpt-tts-notes.md). | Maintainer 2026-07-16 | M-L (phased) | Research done, not scheduled |
> ~~#10: 9 missing building interior images (Alchemist, Market, Archives, Library, Foundry, Warehouse, Keep, Barn, Barracks).~~ — **SHIPPED 2026-06-28** (equipment-items pass + towns/buildings pass). All nine exist in `public/assets/buildings/`, plus ten newly-added building types (apothecary, fletcher, harbormaster, jail, magetower, mill, shrine, stables, tailor, townhall). Coverage is now enforced by `src/utils/buildingArt.test.js` (every placeable building must have an icon + interior image). [MISSING_BUILDING_IMAGES.md](MISSING_BUILDING_IMAGES.md) already records this.

> ~~#46: Balance-sim harness.~~ — **SHIPPED 2026-07-03** (`75bbdc6`). `src/game/balanceSim.js` + `balanceSim.test.js` (Monte-Carlo over the real resolver, KNOWN_UNBALANCED guard, Lead/Support + boss-profile modeling) and the six-guard progression lint (`src/game/progressionLint.test.js`).

> ~~#47: Leveling grants zero combat power.~~ — **SHIPPED 2026-07-03** (`fb7515f`, decision in `2fa4d09`): Option A, a level term of +1 per 2 levels (capped +3) on every check in `encounterResolver.js`, derived from level so it applies retroactively to existing saves. The ASI-at-Lv-4 modal was considered and **deferred** as a possible later layer, not part of this row.

> ~~#48: Level-up can LOWER max HP.~~ — **FIXED 2026-07-03** (`17f33bc`). `progressionSystem.js` now resolves `heroClass` (with `characterClass` fallback via `resolveClass`), unifies the maxHP formula, and clamps recalculated maxHP with `Math.max(recalc, current)` so older already-lowered saves heal forward.

> ~~#49: Unobtainable gear, dead site-loot pools, wrong quest hints.~~ — **FIXED 2026-07-03** (`b9a23a4`). `sitePopulator.js` no longer coerces forest/hills/mountain sites to 'cave' for loot (`LOOT[site.type]` used directly; only combat templates legitimately borrow cave/ruins mobs); themed pools + `HOARD_BONUS` now roll, gear made obtainable, and the obtainability lint guard landed with #46. Loot rarity is additionally tier/level-gated in `encounterResolver.js`.

> ~~#41/#42: Journal side-quest tab + objective clarity.~~ — **SHIPPED 2026-07-03.** Journal has 📜 Campaign | 🗺️ Side Quests tabs; quest cards sort ready→active→completed with per-step progress and derived how/where hints (questHints.js), plus the accept-time site-reveal toast. Design record: [SIDEQUEST_UX_PLAN.md](SIDEQUEST_UX_PLAN.md).

> ~~#34: New Game custom-game options are drop-down heavy; player suggested collapsible accordion sections.~~ — **RESOLVED 2026-07-02 (Skip the accordion).** The real complaint ("a lot of drop-downs" / overload) was addressed by replacing the 5 tone dials (Grimness/Darkness/Magic/Tech/Narrative Style) with segmented **chip rows** (every option visible, one tap) plus sticky theme defaults + a "reset to theme" link. An accordion was evaluated and rejected: it would hide the main quest-builder behind clicks and broke the live preview. The quest-slot pickers keep dropdowns (many options each, where a dropdown is the right control). No further work planned. (The same feedback floated portrait-matches-race, already out of scope: race selector is hidden / human-only.)

---

## Small — polish, loose ends, decisions

| # | Issue | Source | Size | Decision |
|---|---|---|---|---|
| 16 | **Structured logging + request correlation IDs.** Worker uses `console.error`; no request-ID middleware, no log aggregation verified. | [DEPLOYMENT_ARCHITECTURE.md](DEPLOYMENT_ARCHITECTURE.md) §5 | S | |
| 17 | ~~**RAG index cleanup on save delete.**~~ **DONE 2026-07-06.** `conversationsApi.remove()` now purges the session's vectors from the RAG IndexedDB (`ragStore.clearSession`), best-effort so a purge failure never fails the delete. Guests included (the local-only delete path purges too); parked diverged copies (`<sid>-local-<rand>`) are their own session ids, so deleting one through the same path purges exactly its own index. | [LOCAL_RAG_PLAN.md](LOCAL_RAG_PLAN.md) Q5 | XS | Done |
| 18 | ~~**Embedding model migration strategy.**~~ **DONE 2026-07-06.** New vectors carry a `modelVersion` stamp (`EMBEDDING_MODEL_VERSION` in `src/game/ragEngine.js`, mirroring `EMBEDDING_MODEL` in `cf-worker/src/routes/embed.ts`; bump both together). Policy: missing stamp = current (pre-stamp vectors, no migration); mismatched stamp = excluded from retrieval (logged once per session) and counted as unindexed by `getIndexStatus`/`backfill`, so the existing useRagSync load-time auto-backfill re-embeds it in place (same entry id). | [LOCAL_RAG_PLAN.md](LOCAL_RAG_PLAN.md) Q7 | S | Done |
| 19 | **Ops runbook.** No `docs/ops_runbook.md` for incidents, rollback, webhook replay. | [DEPLOYMENT_ARCHITECTURE.md](DEPLOYMENT_ARCHITECTURE.md) §5 | S | Done (merged 2026-07-06): docs/OPS_RUNBOOK.md |
| 20 | **Backup restore drill.** Supabase provides backups; documented restore test not recorded. | [DEPLOYMENT_ARCHITECTURE.md](DEPLOYMENT_ARCHITECTURE.md) §5 | S | Done (merged 2026-07-06): scripts/ops/backup-postgres.sh + restore drill (runbook sections 6-7) |
| 21 | **Smoke-test-on-deploy.** Playwright runs on PR/push but not gated against post-deploy production. | [DEPLOYMENT_ARCHITECTURE.md](DEPLOYMENT_ARCHITECTURE.md) §5 | S | Done (merged 2026-07-06): scripts/ops/smoke-prod.mjs, wired as the deploy workflow's final step |
| ~~35~~ | ~~**Narrative-tier encounter hooks have no visible affordance (signed-in).**~~ **SHIPPED 2026-07-03.** After a Look-around narration consumes a parked hook, the encounter's `suggestedActions` render as tappable chips (+ an Ignore chip) under that narration message (`NarrativeHookChips.js`); tapping one opens the real action modal, so skill rolls and rewards are reachable without typing. Chip state is transient (message-identity keyed in `Game.js`), never persisted into saved conversations. Guests keep the 2026-07-02 fallback modal. | Playtest 2026-07-02 (hills treasure) | S-M (design + UI) | Shipped |
| ~~36~~ | ~~**Parked Look-around encounter silently discarded on next move.**~~ **SHIPPED 2026-07-03.** `pendingLookEncounter` now survives `NARRATIVE_HOOK_PERSIST_MOVES` (3) world moves (`ageNarrativeHook` in `encounterController.js`): one subtle reminder line on the first move away, then silent expiry so stale hooks can't teleport. A failed/empty Look-around re-parks the hook instead of losing it. Offered chips (#35) expire on the same window. | Playtest 2026-07-02 | XS-S | Shipped |
| ~~37~~ | ~~**Encounter presentation note: narrative-tier hooks show no encounter image.**~~ **SHIPPED 2026-07-03.** The #35 chips block shows a modest encounter image preview (max-height 90px) above the chip row when the template has `image:`; the action modal still carries the full art. | Playtest 2026-07-02 | XS (note) | Shipped |
| 39 | **Server-side entitlements (prerequisite for charging money).** ~~`isPremium()` is a client-side localStorage placeholder — one console line unlocks everything.~~ **BUILT 2026-07-05 (pending review + manual migration).** Tier storage: `account_tiers` table in the data Postgres, account-scoped and game-agnostic (user_id = auth-hub JWT `sub`, tier only; hoistable to the Octonion hub later). Migration ships as a FILE with its runbook in the header comment: `cf-worker/migrations/002_account_tiers.sql` (maintainer applies via psql; manual grants via the INSERT ... ON CONFLICT recipe there). Worker: `GET /api/db/entitlements` (authed group, read-only; no row = `{ tier: 'free' }`); smoke test `scripts/test-cf-entitlements.mjs`. Client: `src/game/entitlements.js` is tier-aware (`TIER_LADDER` free<member<premium<elite, `hasTier`, `getUserTier` fetched once per session + localStorage mirror, guests/failures = free); `isPremium()` = member-or-above so every existing gate flows from the real tier; desert/snow themes + premium templates gate at 'member'; `WATER_TOWN_ARCHETYPES`/`canUseWaterTown` staged (riverfork: member, canal: premium). Fetch triggers in `AuthContext` on sign-in, clears on sign-out; dev override kept, compiled out of production like the debug-routes flag. Deferred: PUT/admin endpoint (grants stay psql-only until billing #6), Worker-side enforcement of premium content delivery (#40), hub hoisting. Client gating stays UX only. | [LICENSING_OPTIONS.md](LICENSING_OPTIONS.md) decision 2026-07-02 | M | Built (pending review + migration) |
| 40 | **Server-delivered premium content channel.** Future premium campaigns/music must NOT ship in the public repo or client bundle (anything bundled is public regardless of license). **TEMPLATE DELIVERY BUILT 2026-07-05 (pending review + manual migration).** Storage: `premium_templates` table in the data Postgres (id, min_tier on the #39 ladder, template JSONB = the storyTemplates entry verbatim, enabled kill switch); migration ships as a FILE with its runbook in the header: `cf-worker/migrations/004_premium_templates.sql` (maintainer applies via psql; template load recipes (psql `\set`, and a `psql -v` one-liner reading a JSON file from the private content repo) and the disable recipe live there). Worker: `GET /api/db/premium-templates` (authed group, read-only) joins the caller's `account_tiers` rank and returns `{ templates: [...] }` with all enabled rows: full templates where the tier covers min_tier, and marketing-safe card-face-only teasers (`teaser: true`, no authored content) for rows above tier, so unentitled players see upsell cards (shipped 2026-07-06, PR #53); never an error. Smoke test `scripts/test-cf-premium.mjs` (manual, not auto-run). Client: `src/services/premiumContentApi.js` loads once per session after sign-in (AuthContext), registers via `storyTemplates.registerPremiumTemplates` (idempotent by id; precedence local dev slot > server-delivered > built-in) and caches in sessionStorage only, so a lapsed account's next session starts clean; guests/fetch failures = built-ins (fail closed, quiet). Delivered templates carry their own `premium` flags and flow through the existing canUseTemplate gating in NewGame. The predeploy guard (`scripts/check-no-premium-slot.mjs`) still protects the local slot. Deferred: music via gated R2/CDN URLs, admin write endpoint (psql-only until billing #6); the locked-stub storefront catalog shipped 2026-07-06 as below-tier teaser cards (PR #53), rendered through the existing locked-card treatment. The already-published desert/snow campaigns stay gated in-product as-is (JSON is irrevocably Apache; treat as free marketing). | [LICENSING_OPTIONS.md](LICENSING_OPTIONS.md) decision 2026-07-02 | M | Built (pending review + migration) |

> ~~#13: Narrative encounter engagement detection (fragile "approach"/"investigate" keyword matching).~~ — **SUPERSEDED (verified 2026-07-03 audit).** The keyword matching no longer exists. Encounter delivery was redesigned into two tiers ([TIERED_NARRATION_PLAN.md](TIERED_NARRATION_PLAN.md) B3b): immediate-tier opens the action modal; narrative-tier is parked as `pendingLookEncounter` and consumed by the Look-around prompt. The *remaining* engagement gap (no affordance to act on a woven hook, hook lost on move) is exactly #35-#37 — track it there.

> ~~#14: Immediate vs narrative encounter prompt suppression (decision pending).~~ — **DECIDED & SHIPPED (verified 2026-07-03 audit).** Movement no longer auto-fires AI narration at all (B3b "smart" narration: local templated lines per move; AI only on Look-around / free-text). When an immediate encounter fires, the flow returns early and local narration resumes after resolution (`Game.js` `flowType === 'immediate'`), so the old question is moot.

> ~~#15: 4 missing quest item icons (`medical_journal`, `medicine_kit`, `uncovered_ruins`, `nature_blessing`).~~ — **SHIPPED by 2026-06-28** (equipment-items pass `9782920`). All four exist in `public/assets/icons/items/`.

---

## Playtest feedback — Henry (2026-06-19)

Raw notes captured during a play session. Three items already fixed: hero cards stretched full-width when only one hero was present (now capped/card-sized); the Arcane Renaissance setting text named only Cogsworth while the first objective is in Tinker-Row (setting now points to the Tinker-Row workshop); and the missing-workshop bug (#22).

> ~~#22 Quest-blocking: workshop / quest building missing on some maps~~ — **fixed 2026-06-19.** Root cause: world-map town count was a random 2-4 independent of how many named towns the campaign requires, so milestone towns (Tinker-Row/Brasswick/Gear-End) and their quest buildings were silently dropped. `generateMapData` now guarantees at least as many towns as the campaign's named-town count (relaxing spacing on crowded maps), and `spawnWorldMapEntities` warns when a quest building targets a town that isn't on the map. Covered by a new `mapGenerator.test.js` case.

> ~~#23 Post-creation dead end~~ — **fixed 2026-06-20.** Added a guided onboarding flow: a shared `OnboardingSteps` indicator (Create Hero → Start a Game → Begin Quest) across the setup pages (marking a step done only when truly complete, e.g. step 1 only once a hero exists); an honest HeroSummary button label; a "Start a New Game →" next-step banner + real empty state on the roster (spotlighted right after adding a hero); a dismissible "New here?" prompt on the home page (auto-hidden once the player has saved games); and an empty-state CTA on Saved Games. Also fixed a misleading new-game validation message and renamed the "Templates" tab to "Ready-Made".

> ~~#24 Character selection process not obvious~~ — **fixed 2026-06-21.** Hero Selection now has a clear instruction line, a live "Party: N of 4 selected" counter, a per-card Add / "✓ In party" pill (with hover state and a dimmed "Party full" state at the 4-hero limit), and the cards are keyboard-operable buttons (role/tabindex/aria-pressed, Enter/Space, focus ring).

> ~~#25 Map discoverability~~ — **addressed 2026-06-21** (guided-tour commit `bc317d4`) and verified 2026-07-03: the onboarding tour has an explicit `open-map` step spotlighting the always-visible header Map button ("Open the map and click a tile to move. It stays open as you explore"), the map stays open while travelling, and it auto-reopens after encounters (`reopenMapAfterEncounterRef`). The "visible at all times" (minimap) alternative was not built; reopen as a new row if playtests still show confusion.

> ~~#26 Missing `workshop` building interior image~~ — **SHIPPED 2026-06-28** (buildings pass `85a027f`/`9782920`): `public/assets/buildings/workshop.webp` exists, and `buildingArt.test.js` now enforces icon+interior coverage for every placeable building type, so this class of bug can't silently recur. All Henry playtest items are now closed.

---

## Playtest feedback — maintainer (2026-07-07, frozen-frontier-t1 on live)

| # | Issue | Source | Size | Decision |
|---|---|---|---|---|
| 74 | ~~**Quest-blocking: "no Trading Post building" in the milestone town.**~~ **FIXED 2026-07-07.** Root cause: New Game hands its optional preview map to `launchCampaign` verbatim (`options.mapData`), and `applyTemplate` never invalidated a preview built under different customNames/theme; a world previewed BEFORE picking Frozen Frontier has no Hearthmere on it, so `spawnWorldMapEntities` could not anchor milestone 1's `warehouse` venue ("The Hearthmere Trading Post") to any town and the quest building (plus all milestone POIs/NPCs) silently never spawned. Same latent hole applied to every template (desert, eldritch, delivered tidewater) via the same stale-preview path. Fix: (a) `launchCampaign` now validates a provided map with `findMissingMilestoneLocations` (milestoneSpawner) and discards + regenerates any map missing a milestone location, mirroring campaignChain's `isTemplateCompatibleWithWorld`; compatible previews are kept byte-identical; (b) `applyTemplate` drops the stale preview (seed kept so "Build Map from Seed" reproduces it under the new names). Pinned by `questBuildingIntegrity.test.js`: the exact playtest repro, a seed-invariant sweep of every launchable template's building milestones on fresh AND stale-preview launches, the kept-valid-preview case, and the server-delivered mechanism (authored venue names, unknown building type). | Playtest 2026-07-07 | S | Done |
| 75 | ~~**Start Adventure button vanished after the first start call failed.**~~ **FIXED 2026-07-07.** `handleStartAdventure` set `hasAdventureStarted` TRUE before the AI call; on failure (the maxTokens-1600 worker 400, fixed separately in `0a63969`) the catch surfaced an error but never reset the gate, and GameMainPanel hides the Start button once `hasAdventureStarted` is true, so the fail permanently removed the button (the guard at the top of the handler also blocked retries). Fix: a failed initial generation (thrown error OR empty AI response) resets `hasAdventureStarted` to false, keeps the error surfaced, and leaves the button in place so a retry runs the full start flow. Covered by `useGameInteraction.startAdventure.test.js` (rejecting AI call → gate stays false + retry fires; empty response; success unchanged). | Playtest 2026-07-07 | XS | Done |

---

## Combat-system depth (competitor-analysis gaps, 2026-06-20)

Surfaced while positioning DungeonGPT against group-2 ("real mechanics") AI-GM competitors — see [ai-game-master-competitor-analysis.md](../ai-game-master-competitor-analysis.md). Combat is already on the defensible *deterministic* side (rolls/damage computed in code, AI does not decide outcomes), but the rules layer is thinner than the combat-fidelity leaders (LoreKeeper, Friends & Fables). Each item below is "fix, or do a thorough check of current behaviour and decide deliberately" — not all are wanted.

| # | Issue | Source | Size | Decision |
|---|---|---|---|---|
| 27 | **No attack-roll / AC / saving-throw loop.** Resolution is a single d20-vs-DC check (`encounterResolver.js`), not attack-roll-vs-armor-class + saving throws. Adds the biggest fidelity gap vs 5e-style competitors. | Competitor analysis | L | |
| 28 | **No conditions / status effects.** No buffs/debuffs, concentration, poison/stun/ongoing effects — only direct HP damage (`healthSystem.js`). | Competitor analysis | M-L | |
| 29 | **No real initiative / turn order.** Only a ~15% fumble check on action confirmation (`EncounterActionModal.js`); no initiative roll or turn sequencing. | Competitor analysis | M | |
| 30 | **No death saves / downed state.** HP ≤ 0 flips `isDefeated: true` and ends the encounter — no stabilisation, revival, or party-rescue mechanics. | Competitor analysis | M | |
| 31 | **No tactical positioning / battlemaps.** Encounters are abstract, not spatial; no token-on-grid layer (Friends & Fables has one). | Competitor analysis | L | |
| 32 | **No real-time multiplayer.** Single-player only. Flagged in the analysis as the largest strategic moat (Whitespace #1) and the most expensive gap to close on our stack. | Competitor analysis | XL (strategic) | |
| 33 | **Combat state only partially visible to the AI for later narrative.** Verify/decide what to pass. Today: encounter narration goes into conversation history + RAG, and defeated heroes show `[DEFEATED]` in the party list — but exact HP numbers, per-round damage dealt, and loot gained are **not** put in later prompts, so the AI is blind to tactical detail when narrating subsequent movement/location/story. | Session 2026-06-20 | S-M (design/check) | Partially done |

> **#33 — partially addressed 2026-06-21.** `formatPartyInfo` (`promptComposer.js`) now appends a coarse condition band for wounded heroes — `[injured]` (≤75%), `[badly wounded]` (≤50%), `[critically wounded - near death]` (≤25%) — reusing `getHPStatus`; full-health heroes stay plain and **no raw HP numbers** reach the AI. Also consolidated the two inline party formatters in `useGameInteraction.js` (adventure-start + player-action prompts) onto `formatPartyInfo`; these previously omitted even the `[DEFEATED]` flag, so an unconscious hero could be narrated as acting. Unit tests added to `promptComposer.test.js`. **Still open:** per-round damage and loot-gained (loot = #8) are still not surfaced; no integration/hook-level test proves the band reaches the live prompt; deeper "what combat detail should the AI see" remains a design call.

> **Not a deficiency — confirmed intentional:** combat narration is fully local/templated with **zero LLM calls** (`encounterResolver.js:53` — "Use base consequence for narration (fully local, no AI calls)"). This is a deliberate cost/latency win and should be preserved; the open question (#33) is only how much resolved *state* to surface to the AI for later actions, not whether to have the AI narrate combat itself.

---

## Parallelization map (audit 2026-07-03)

*(Historical: kept as the cluster/file-ownership record. For ordering, use the "Priority queue" section at the top of this doc, added 2026-07-16; several rows referenced below have since shipped.)* *(Replaces the stale "Recommended prioritization" section, which referenced already-resolved rows.)* Groups the remaining open items into clusters by file/system ownership, the same wave model used for #46-#49. Items in different clusters can run as parallel agent-sized packages; items inside a "serialize" cluster must land in order. Numbering caveat: **#30 is used twice** in this doc (admin dashboard, and no-death-saves in the combat table) — do not renumber; qualify references as "#30 (admin)" vs "#30 (combat)".

**A. Combat core — SERIALIZE (owns `encounterResolver.js`, `EncounterActionModal.js`, encounter data, `balanceSim.js`):**
- #43 (in progress) → #44 (gear-ladder tuning; needs #43 so defense matters) → t3 authoring parts of #45/#50.
- #27, #28, #29, #30 (combat), and the remainder of #33 are all reshaped by #43's outcome (boss damage profiles, multi-round fights); re-triage them after #43 lands rather than starting any now.

**B. Content / data-only — PARALLEL-SAFE (owns `src/data/` quests/templates; no engine files):**
- #45 side-quest pool expansion + backfill, #50 mid-band quests/sequels/random-encounter tiers (the two may merge). Data authoring can start now; the t3 entry-range and DC-ladder *decisions* wait on cluster A. The #46 lint guards keep this cluster honest.
- #9 world-map AI tiles (asset generation + `worldTileArt` wiring) — fully independent.

**C. Worker / backend: unblocked 2026-07-05 (the Hyperdrive migration is committed and live):**
- #39 entitlements (BUILT, pending review + manual `002_account_tiers.sql` migration) → #40 premium content channel (template delivery BUILT, pending review + manual `004_premium_templates.sql` migration) → #6 billing → #7 OpenRouter (**#7 MERGED 2026-07-06, PR #52** riding #39's tier gating directly; deploy prereqs: manual `005_request_counters.sql` migration + `OPENROUTER_API_KEY` secret; #6 billing remains the missing piece for paid signups, and #40 is still needed before the next premium campaign is authored).
- #12 rate limiting (**MERGED 2026-07-06, PR #52** with #7, shared `request_counters` infrastructure; deploy prereq: manual migration 005), #16 structured logging, #30 (admin) dashboard — the latter two still independent, both touch the worker/DB layer.

**D. Encounter-UX package — PARALLEL to everything else (owns `Game.js` encounter flow + modal):**
- #35 + #36 + #37 as one package (#37 explicitly depends on #35's affordance; #36 shares the `pendingLookEncounter` lifecycle). #8 (loot narration) also lives in this surface and can ride along or follow.
- Caveat: if #35's fix opens the action modal, it touches `EncounterActionModal.js`, which cluster A (#43) is actively rewriting — sequence that specific piece after #43 lands, or scope #35 to chips-under-narration only.

**E. Ops / infra — PARALLEL-SAFE, each independent:**
- #11 auto-deploy, #19 runbook, #20 restore drill, #21 smoke-on-deploy, #17 RAG cleanup, #18 embedding migration design, #1 (Supabase dashboard template edit, 5 min, no code).

**F. Big bets — own tracks, not scheduled:**
- #3 (absorbed into #43), #5 layered terrain (map-gen only; safe alongside all clusters but not with other world-gen changes), #31 battlemaps, #32 multiplayer.
